{
  "name": "axm-material-surface-fabric",
  "version": "0.4.0",
  "private": true,
  "description": "Local-first material/texture composition, observable state tracing, spatial delta experiments, persistent reusable library, and donor-pack reuse for AXM.",
  "scripts": {
    "test": "node tests/state-core.test.js && node tests/trace-core.test.js && node tests/delta-core.test.js && node tests/library-core.test.js"
  }
}
