# Signal-audit examples

This directory is intentionally text-only for v0.18.6. The real asset-production batch is not copied here before it exists.

Use `npm run material:signal -- audit <library.json> <report.json>` on the real Material Library snapshot when that batch lands. Reports distinguish payload/container failure from suspicious pixel-signal state, but they do not score beauty or compare against a game reference automatically.
