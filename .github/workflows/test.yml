name: test

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  state-core:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: JavaScript syntax
        run: |
          node --check app.js
          node --check state-core.js
      - name: State-core tests
        run: npm test
